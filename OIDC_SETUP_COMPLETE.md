# ✅ NuGet Trusted Publishing (OIDC) 配置完成

## 🎉 已完成的工作

### 📁 创建/更新的文件

1. **`.github/workflows/build_nuget.yml`** ✨ 已更新
   - 使用 NuGet Trusted Publishing (OIDC)
   - 无需手动管理 API Key
   - 支持 Git 标签自动触发
   - 支持手动触发

2. **`NUGET_TRUSTED_PUBLISHING_GUIDE.md`** 📖 详细指南
   - 完整的配置步骤
   - 工作原理说明
   - 常见问题解答
   - 安全最佳实践

3. **`OIDC_QUICK_SETUP.md`** 🚀 快速配置
   - 3 步完成配置
   - 清晰的检查清单
   - 快速故障排除

---

## 🆚 与传统方案的对比

### 之前：传统 API Key 方式
```yaml
# 需要在 GitHub Secrets 中存储长期 API Key
- name: 发布到 NuGet.org
  run: dotnet nuget push ... --api-key ${{ secrets.NUGET_API_KEY }}
```
❌ 需要手动创建和存储 API Key  
❌ API Key 泄露风险高  
❌ 需要定期轮换密钥  

### 现在：OIDC Trusted Publishing ✨
```yaml
# 使用 OIDC 自动获取临时令牌
- name: NuGet 登录 (OIDC → 临时 API Key)
  uses: NuGet/login@v1
  with:
    user: ${{ secrets.NUGET_USER }}

- name: 发布到 NuGet.org
  run: dotnet nuget push ... --api-key ${{ steps.nuget_login.outputs.NUGET_API_KEY }}
```
✅ 无需管理长期密钥  
✅ 自动获取临时令牌  
✅ 更高的安全性  
✅ 完整的审计跟踪  

---

## ⚡ 现在您需要做的（3 步）

### 第 1 步：在 NuGet.org 配置 Trusted Publisher

```
访问: https://www.nuget.org/packages/Arc.UniInk/manage
（如果包还不存在，需要先发布首个版本）

点击 "Add trusted publisher"

填写信息：
  Publisher type: GitHub Actions
  Owner: Arc-huangjingtong
  Repository: UniInk-CSharpInterpreter4AOT
  Workflow: build_nuget.yml
  Environment: (留空)

点击 "Add"
```

### 第 2 步：在 GitHub 添加 Secret

```
仓库 Settings → Secrets and variables → Actions → New secret

Name: NUGET_USER
Value: [您的 NuGet.org 用户名]

点击 "Add secret"
```

💡 **查找用户名**: https://www.nuget.org/account

### 第 3 步：推送并测试

```bash
# 推送工作流文件
git add .github/workflows/build_nuget.yml
git add NUGET_TRUSTED_PUBLISHING_GUIDE.md
git add OIDC_QUICK_SETUP.md
git commit -m "配置 NuGet Trusted Publishing (OIDC)"
git push origin main

# 测试发布
git tag v0.1.0
git push origin v0.1.0
```

---

## 🎯 工作流特性

### ✨ 触发方式

#### 1. Git 标签自动触发（推荐）
```bash
git tag v0.1.0
git push origin v0.1.0
```

#### 2. 手动触发
```
GitHub → Actions → "Build and Publish NuGet (OIDC)" → Run workflow
```

### 📋 自动化流程

当推送标签时，GitHub Actions 会自动：

1. ✅ **检出代码** - 获取最新代码
2. ✅ **提取版本号** - 从标签中提取版本
3. ✅ **更新版本** - 自动更新 csproj 中的版本号
4. ✅ **还原依赖** - 下载 NuGet 包
5. ✅ **构建项目** - Release 配置
6. ✅ **运行测试** - 如果存在测试项目
7. ✅ **打包 NuGet** - 包含 XML 文档和符号包
8. ✅ **OIDC 认证** - 获取临时 API Key
9. ✅ **发布到 NuGet** - 自动推送包
10. ✅ **上传产物** - 保存到 GitHub Artifacts
11. ✅ **创建 Release** - 自动创建 GitHub Release

---

## 🔐 OIDC 安全优势

### 传统 API Key 的风险

```
长期有效的 API Key
    ↓
存储在 GitHub Secrets
    ↓
如果泄露，攻击者可以：
  ❌ 发布恶意包
  ❌ 删除现有版本
  ❌ 修改包信息
```

### OIDC Trusted Publishing 的安全性

```
GitHub Actions 触发
    ↓
GitHub 颁发 OIDC Token（包含仓库、工作流等信息）
    ↓
NuGet.org 验证 Token
    ↓
颁发临时 API Key（仅本次工作流有效）
    ↓
发布完成后，API Key 自动失效
    ↓
✅ 即使泄露也无法重复使用
✅ 完整的审计跟踪
✅ 无需手动管理密钥
```

---

## 📊 工作流程图

```
┌─────────────────────────────────────────────────────────────┐
│                     开发者推送标签                            │
│                   git push origin v0.1.0                    │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                  GitHub Actions 触发                         │
│              工作流: build_nuget.yml                         │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│              GitHub 颁发 OIDC Token                          │
│   包含：仓库、工作流、触发者等身份信息                          │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│            NuGet/login@v1 Action 执行                        │
│    将 OIDC Token 发送到 NuGet.org                            │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│          NuGet.org 验证 OIDC Token                           │
│   检查：仓库、工作流是否在 Trusted Publishers 列表中          │
└──────────────────────────┬──────────────────────────────────┘
                           │
                  验证成功  │  验证失败
                           │            └──→ ❌ 401 Unauthorized
                           ▼
┌─────────────────────────────────────────────────────────────┐
│        NuGet.org 颁发临时 API Key                            │
│         有效期：仅本次工作流运行期间                           │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│            使用临时 API Key 发布包                            │
│     dotnet nuget push ... --api-key [临时Key]               │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                 ✅ 发布成功                                   │
│        临时 API Key 自动失效，无法重复使用                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 📖 使用示例

### 发布新版本（完整流程）

```bash
# 1. 开发新功能
vim Arc.UniInk/Arc.UniInk/UniInk.cs

# 2. 提交更改
git add .
git commit -m "添加新功能 X"

# 3. 创建标签（包含详细说明）
git tag -a v0.2.0 -m "版本 0.2.0

新增功能：
- 新功能 X
- 新功能 Y

Bug 修复：
- 修复问题 A
- 修复问题 B
"

# 4. 推送到 GitHub
git push origin main
git push origin v0.2.0

# 5. GitHub Actions 自动执行
#    - 查看进度：GitHub → Actions → 查看最新运行
#    - 等待 2-5 分钟完成构建和发布

# 6. 验证发布
#    - NuGet: https://www.nuget.org/packages/Arc.UniInk/
#    - GitHub Release: https://github.com/Arc-huangjingtong/UniInk-CSharpInterpreter4AOT/releases
```

### 版本号管理

```bash
# Bug 修复（Patch 版本）
git tag v0.1.1 && git push origin v0.1.1

# 新功能（Minor 版本）
git tag v0.2.0 && git push origin v0.2.0

# 破坏性变更（Major 版本）
git tag v1.0.0 && git push origin v1.0.0

# 预发布版本
git tag v0.2.0-alpha && git push origin v0.2.0-alpha
git tag v0.2.0-beta && git push origin v0.2.0-beta
git tag v1.0.0-rc.1 && git push origin v1.0.0-rc.1
```

---

## ❓ 常见问题

### ❓ 首次发布怎么办？

NuGet Trusted Publishing 要求包已存在才能配置。

**解决方案 1：手动发布首个版本**
```bash
# 1. 构建包
dotnet pack Arc.UniInk/Arc.UniInk/Arc.UniInk.csproj -c Release

# 2. 访问 https://www.nuget.org/packages/manage/upload
#    上传生成的 .nupkg 文件

# 3. 发布成功后，配置 Trusted Publisher
#    然后就可以使用 GitHub Actions 了
```

**解决方案 2：临时使用传统 API Key**
```bash
# 1. 创建临时 API Key（有效期设短一些）
# 2. 使用 API Key 发布首个版本
# 3. 配置 Trusted Publisher
# 4. 撤销临时 API Key
```

### ❓ OIDC 认证失败怎么办？

**错误**: `Failed to authenticate with OIDC` 或 `401 Unauthorized`

**检查清单**:
- [ ] NuGet.org 上是否配置了 Trusted Publisher？
- [ ] 配置中的 Owner、Repository、Workflow 是否完全匹配？
- [ ] GitHub Secrets 中的 `NUGET_USER` 是否正确？
- [ ] 工作流是否有 `permissions: id-token: write`？
- [ ] 包是否已存在于 NuGet.org？

### ❓ 可以同时保留传统 API Key 吗？

**可以**！两种方式可以共存：
- GitHub Actions 使用 OIDC（推荐）
- 本地开发使用传统 API Key
- 两者互不影响

### ❓ 如何撤销 Trusted Publisher 配置？

```
1. 访问包管理页面
   https://www.nuget.org/packages/Arc.UniInk/manage
2. 找到 "Trusted publishers" 部分
3. 点击对应配置旁的 "Remove" 按钮
```

---

## 📚 相关文档

| 文档 | 说明 |
|------|------|
| `NUGET_TRUSTED_PUBLISHING_GUIDE.md` | 完整的配置和使用指南 |
| `OIDC_QUICK_SETUP.md` | 3 步快速配置 |
| `GITHUB_ACTIONS_GUIDE.md` | GitHub Actions 通用指南 |
| `QUICK_PUBLISH_GUIDE.md` | 快速发布参考 |

### 官方文档

- 📖 [NuGet Trusted Publishing](https://learn.microsoft.com/zh-cn/nuget/nuget-org/trusted-publishing)
- 🔐 [GitHub OIDC](https://docs.github.com/en/actions/deployment/security-hardening-your-deployments/about-security-hardening-with-openid-connect)
- 🔗 [NuGet/login Action](https://github.com/NuGet/login)

---

## 🎉 总结

### 优势

✅ **安全** - 无需存储长期 API Key  
✅ **简单** - 配置一次，永久有效  
✅ **自动** - 推送标签即可发布  
✅ **可追溯** - 完整的审计日志  

### 使用流程

```bash
# 配置（仅第一次）
1. NuGet.org 配置 Trusted Publisher
2. GitHub 添加 NUGET_USER Secret

# 日常发布（每次）
git tag v0.x.x
git push origin v0.x.x
```

**就是这么简单！** 🚀

---

## 📞 需要帮助？

如果遇到问题：
1. 查看 `NUGET_TRUSTED_PUBLISHING_GUIDE.md` 中的常见问题
2. 检查 GitHub Actions 运行日志
3. 访问 NuGet.org 查看 Trusted Publisher 配置
4. 参考官方文档

---

*配置完成后，享受自动化发布带来的便利！* 🎊